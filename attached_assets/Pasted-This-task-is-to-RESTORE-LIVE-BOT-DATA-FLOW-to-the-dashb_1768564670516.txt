This task is to RESTORE LIVE BOT DATA FLOW to the dashboard.

Do NOT change UI.
Do NOT change dashboard layout.
Do NOT reduce displayed information.

=====================================
STEP 1: BOT ENGINE START RESTORATION
=====================================

In main.py, explicitly start ALL background engines:

- session_engine.start()
- market_data_and_signal.start_market_scan()
- trade_management_and_risk.start_monitoring()
- event_logger.start_stream()

These must run in background threads or async tasks.

=====================================
STEP 2: STATE MANAGER CONTINUOUS UPDATE
=====================================

Ensure state_manager is updated EVERY cycle with:

- current_market
- market_mode
- signal_type
- confidence
- indicator_logic (human readable)
- trade_decision
- rejection_reason
- risk_score
- active_positions
- narrative_logs (rolling)

No placeholder values allowed after boot.

=====================================
STEP 3: DASHBOARD DATA SOURCE CHECK
=====================================

Dashboard must read ONLY from state_manager.

Do NOT hardcode defaults like:
"-", 0, empty arrays

If data not ready:
show "INITIALIZING" instead.

=====================================
STEP 4: REMOVE BOOT LOCK
=====================================

After first successful market scan:
Set system state from:
BOOTING → ACTIVE

This must be automatic.

=====================================
STEP 5: LOG STREAM FIX
=====================================

Narrative Logs panel must stream:
- market scan messages
- signal evaluation
- trade rejection reasons
- trade open/close events

At least 1 log every scan cycle.

=====================================
STEP 6: VALIDATION
=====================================

After fix:
- Current Market must show symbol
- Confidence > 0 when signal exists
- Risk Score must change dynamically
- Narrative Logs must scroll
- Active Positions must update live

If any value remains static:
STOP and report error.

=====================================
RULES
=====================================

- Do not simplify the dashboard
- Restore full data richness (as before)
- Logic > visuals
- No silent failures

End task.
